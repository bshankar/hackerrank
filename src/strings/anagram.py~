#!/usr/bin/python3

# https://www.hackerrank.com/challenges/anagram/problem


def min_steps_to_anagram(s):
    if len(s) % 2 != 0:
        return -1

    s1 = sorted(s[:len(s) // 2])
    s2 = sorted(s[len(s) // 2:])
