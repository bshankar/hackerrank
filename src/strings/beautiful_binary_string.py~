#!/usr/bin/python3

# https://www.hackerrank.com/challenges/beautiful-binary-string/problem

size = input().strip()
s = input().strip()

changes = 0

while s.find("010") != -1:
    s = s.replace("010", "011")
    changes += 1
